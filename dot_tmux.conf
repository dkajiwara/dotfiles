# ============================================================
# Basic Settings
# ============================================================
# デフォルトシェル
set-option -g default-shell /bin/zsh

# 256色表示
set-option -g default-terminal screen-256color
set -g terminal-overrides 'xterm:colors=256'

# ESC キーの遅延を解消（Vim 使用時に重要）
set -s escape-time 0

# ============================================================
# Key Bindings
# ============================================================
# prefix キーを C-a に変更
set -g prefix C-a
unbind C-b

# 設定リロード
bind-key r source-file ~/.tmux.conf \; display-message "~/.tmux.conf reloaded"

# Vim ライクなペイン選択
bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R

# ============================================================
# Status Bar
# ============================================================
# ステータスバーを上部に配置
set-option -g status-position top

# ステータスバーの長さ
set-option -g status-left-length 90
set-option -g status-right-length 90

# ステータスバー更新間隔（秒）
set-option -g status-interval 1

# ウィンドウリストを左揃え
set-option -g status-justify left

# ============================================================
# Mouse
# ============================================================
set-option -g mouse on
bind-key -n WheelUpPane if-shell -F -t = "#{mouse_any_flag}" "send-keys -M" "if -Ft= '#{pane_in_mode}' 'send-keys -M' 'select-pane -t=; copy-mode -e; send-keys -M'"
bind-key -n WheelDownPane select-pane -t= \; send-keys -M

# ============================================================
# Copy Mode (Vi style)
# ============================================================
set-window-option -g mode-keys vi
bind -T copy-mode-vi v send -X begin-selection

# ============================================================
# Plugins
# ============================================================
set -g @plugin 'tmux-plugins/tmux-sensible'
set -g @plugin 'tmux-plugins/tmux-resurrect'
set -g @plugin 'tmux-plugins/tmux-continuum'

# Kanagawa テーマ
set -g @plugin 'Nybkox/tmux-kanagawa'

# ターミナルの背景色を保持
set -g @kanagawa-ignore-window-colors true

# Powerline シンボルを有効化（タブの角を三角形に）
set -g @kanagawa-show-powerline true

# プラグイン設定（CPU 使用率、RAM 使用率、SSH セッション、時間を表示）
set -g @kanagawa-plugins "cpu-usage ram-usage ssh-session time"

# 空のプラグインを非表示
set -g @kanagawa-show-empty-plugins true

# 24時間表記
set -g @kanagawa-military-time true

# tmuxスタート時に自動で復元
set -g @continuum-restore 'on'

# ============================================================
# TPM (Tmux Plugin Manager)
# ============================================================
# TPM 自動インストール
if "test ! -d ~/.tmux/plugins/tpm" \
   "run 'git clone https://github.com/tmux-plugins/tpm ~/.tmux/plugins/tpm && ~/.tmux/plugins/tpm/bin/install_plugins'"

# TPM 初期化（この行は常に最後に配置）
run '~/.tmux/plugins/tpm/tpm'

# ============================================================
# Theme Overrides
# ============================================================
# ペインボーダーの色設定（TPM 初期化後に記述してテーマの設定を上書き）
set-option -g pane-border-style "fg=#54546D"
set-option -g pane-active-border-style "fg=#a3d4d5"

# ズーム状態の表示（ズーム中は虫眼鏡アイコンを status-left に表示）
set-option -ag status-left "#{?window_zoomed_flag,#[fg=#ff9e3b] 🔍,}"
